using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Object_Scripts : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("저는 큐브입니다.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
